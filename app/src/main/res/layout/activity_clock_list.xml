<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/color_menu"
        android:fitsSystemWindows="true"
        android:theme="@style/Theme.ToolBar.Clock"></android.support.v7.widget.Toolbar>

    <ListView
        android:id="@+id/list_view_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:layout_gravity="center"></ListView>



    <ImageView
        android:id="@+id/noclock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/clock"
        android:layout_marginTop="96dp"
        android:layout_centerHorizontal="true" />


    <ImageView
        android:id="@+id/clock_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:layout_marginBottom="42dp"
        android:background="@mipmap/add" />
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/clock_add"
        android:id="@+id/downarrow"
        android:layout_centerHorizontal="true"
        android:src="@drawable/downarrow" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:id="@+id/addtext"
        android:layout_centerHorizontal="true"
        android:text="还没有闹钟，点击星星添加~"
        android:textColor="@color/color_menu"
        android:layout_above="@id/downarrow"
        />



</RelativeLayout>